// contacts.js  —  page controller for index.html
// This file runs when index.html is opened.
// Import the API helpers you need and add your page logic below.

import { getContacts, createContact, deleteContact } from '../api/contacts.js'

// TODO: add your page logic here
// Example steps:
//   1. Call getContacts() and render the results into the page
//   2. Hook up your "Add contact" form to call createContact()
//   3. Hook up delete buttons to call deleteContact(id)
